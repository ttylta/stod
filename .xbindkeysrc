"pactl set-sink-volume @DEFAULT_SINK@ +1000; pulsewatcher up"
   XF86AudioRaiseVolume

# Decrease volume
"pactl set-sink-volume @DEFAULT_SINK@ -1000; pulsewatcher down"
   XF86AudioLowerVolume

# Mute volume
"pactl set-sink-mute @DEFAULT_SINK@ toggle; pulsewatcher mute"
   XF86AudioMute

# Increase backlight
"sudo xbacklight -inc 10"
   XF86MonBrightnessUp

# Descrease backlight
"sudo xbacklight -dec 10"
   XF86MonBrightnessDown
